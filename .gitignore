# Python
__pycache__/
*.py[cod]
*$py.class
*.pyc
.Python

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/*
*-checkpoint.ipynb

# Python scripts (project convention — executed notebooks are the deliverable)
*.py

# Environments
.env
.env.*
!.env.example
.venv/
venv/
env/
ENV/
.conda/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Secrets
*.pem
*.key

# ─────────────────────────────────────────────────────────────
# Raw datasets — large or externally sourced, not committed.
# Download instructions in data/README.md.
# ─────────────────────────────────────────────────────────────
data/MMHS150K/img_resized/
data/MMHS150K/img_txt/
data/MMHS150K/MMHS150K_GT.json
data/HateXplain/
data/The Hateful Memes Challenge/
data/cyberbullying_tweets.csv
*.zip

# ─────────────────────────────────────────────────────────────
# Processed CSVs — large, regenerable from NB 01 / NB 03.
# Small per-sample parquets, identity lexicon, JSONs ARE tracked.
# ─────────────────────────────────────────────────────────────
data/processed/labels_parsed.csv
data/processed/structured_features.csv

# ─────────────────────────────────────────────────────────────
# Model checkpoints — large weight files (.pt and .safetensors).
# Small per-run JSONs / NPYs (metrics, gates, training history,
# standardisation stats, READMEs) ARE tracked for reproducibility.
# ─────────────────────────────────────────────────────────────
models/**/*.pt
models/**/*.safetensors
models/**/tokenizer.json
models/rank64/

# ─────────────────────────────────────────────────────────────
# Logs and scratch — local-only working files
# ─────────────────────────────────────────────────────────────
logs/
*.log
*.tmp
/tmp/
tmp_*
tmp_phase2_additions/

# Local backups + extraction scratch
Reports/_*.md
Reports/*.bak_*

# Reference PDFs (not part of the code repo)
Research Papers/

# ─────────────────────────────────────────────────────────────
# Claude Code local state (per-user, not for shared repo)
# CLAUDE.md contains project-internal rules — kept local-only per
# prior project convention (commit c8ccee4 "Delete CLAUDE.md").
# ─────────────────────────────────────────────────────────────
.claude/
CLAUDE.md
CLAUDE.local.md

# Lightning AI Studio runtime path
/teamspace/
